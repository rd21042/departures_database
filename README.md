# Departure board on OLED Screen
A simple project that takes timetables (as json) and a stop name and prints the departure on a OLED screen wired to a Raspberry Pi Pico
Before usage check which port the Pico is connected to and put the that port name in variable "port" in sender.py